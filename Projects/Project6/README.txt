Open the PROJECT.sln file using Visual Studio to run the full project. Form1.cs can be opened with a text editor to view the code.

This project accepts a text file as input, orders the input, calculates variables, and allows for review by the user.